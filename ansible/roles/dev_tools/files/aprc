# === Dracula Color Pallette ===
#   white='#f8f8f2' 253
#   gray='#44475a' 239
#   dark_gray='#282a36' 236
#   light_purple='#bd93f9' 141
#   dark_purple='#6272a4' 61
#   cyan='#8be9fd' 117
#   green='#50fa7b' 84
#   orange='#ffb86c' 215
#   red='#ff5555' 203
#   pink='#ff79c6' 212
#   yellow='#f1fa8c' 228

AmazingPrint.defaults = {
  :indent     => 4,
  :index      => true,   # Display array indices.
    :html       => false,  # Use ANSI color codes rather than HTML.
    :multiline  => true,   # Display in multiple lines.
    :plain      => false,  # Use colors.
    :raw        => false,  # Do not recursively format object instance variables.
    :sort_keys  => false,  # Do not sort hash keys.
    :limit      => false,  # Limit large output for arrays and hashes. Set to a boolean or integer.
    :color => {
      :args       => :orange,
      :array      => :white,
      :bigdecimal => :blue,
      :class      => :yellow,
      :date       => :greenish,
      :falseclass => :red,
      :fixnum     => :blue,
      :float      => :blue,
      :hash       => :white,
      :keyword    => :cyan,
      :method     => :purpleish,
      :nilclass   => :red,
      :rational   => :blue,
      :string     => :yellowish,
      :struct     => :white,
      :symbol     => :purple,
      :time       => :greenish,
      :trueclass  => :cyan,
      :variable   => :cyanish
    }
}